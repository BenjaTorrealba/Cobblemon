services:
  web:
    build: .
    container_name: cobblemon-web
    ports:
      - "3000:3000"
    environment:
      - HOST=0.0.0.0
      - PORT=3000
    volumes:
      - ./data/dev.db:/app/prisma/dev.db
    env_file:
      - .env
    restart: unless-stopped
